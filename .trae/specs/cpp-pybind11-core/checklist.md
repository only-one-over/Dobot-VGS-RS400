- [x] `cpp_core/CMakeLists.txt` 存在且可正确配置 Pybind11 编译
- [x] `cpp_core/src/pybind_module.cpp` 定义了 `dobot_core` Pybind11 模块入口
- [x] `build_cpp.py` 可一键编译生成 `dobot_core` Python 扩展
- [x] `dobot_core.transforms.euler2rot` 输出与 Python 版本在 1e-6 精度内一致
- [x] `dobot_core.transforms.pose2matrix` 输出与 Python 版本在 1e-6 精度内一致
- [x] `dobot_core.transforms.transform_point` 正确执行齐次变换
- [x] `dobot_core.nms.nms` 返回结果与 Python 版本完全一致
- [x] `dobot_core.yolo.postprocess_yolov8` 检测框坐标差异 < 1 像素
- [x] `dobot_core.yolo.process_mask` 掩码 IoU > 0.95
- [x] `vision_system.py` 优先使用 C++ 实现，不可用时回退到 Python
- [x] `transform_utils.py` 优先使用 C++ 实现，不可用时回退到 Python
- [x] `dobot_core` 不可用时程序仍可正常运行（回退机制生效）
