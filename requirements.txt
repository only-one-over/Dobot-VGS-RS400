# 默认聚合入口 — 安装基础依赖 + GPU 推理依赖
# 用法：pip install -r requirements.txt
-r requirements-base.txt
-r requirements-gpu-cu12.txt

# === 可选：C++ 加速模块 ===
# pip install -r requirements-cpp.txt
# python build_cpp.py
