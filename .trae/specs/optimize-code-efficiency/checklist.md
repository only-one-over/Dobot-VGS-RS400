- [x] `gui_app.py` 中 `json` 和 `os` 已提升到文件顶部导入，方法内部无重复导入
- [x] `gui_app.py` 中默认抓取流程模块列表仅定义一次（模块级常量），`__init__` 中无重复代码
- [x] `gui_app.py` 中 `GripperThread`、`BatteryThread`、`TorqueThread` 已合并为泛型 `MonitorThread`
- [x] `gui_app.py` 中 `on_step_clicked` 仅更新样式，不触发全量 UI 重建
- [x] `robot_controller.py` 中 `config_manager` 导入已提升到文件顶部
- [x] `robot_controller.py` 中错误码描述逻辑已提取为 `_describe_error_code` 方法
- [x] `robot_controller.py` 中 `parse_response_code` 无冗余逻辑
- [x] `vision_system.py` 中 `euler2rot` 和 `pose2matrix` 仅定义一次
- [x] `vision_system.py` 中 `Z_mm = Z_mm` 无效赋值已删除
- [x] `vision_system.py` 中 `MockFrame` 类为模块级定义
- [x] `config_manager.py` 实现了配置缓存，连续读取不触发重复文件 I/O
- [x] `config_manager.py` 写入配置后缓存自动更新
- [x] `dobot_api.py` 中 `send_command` 能完整接收长响应
- [x] `test_gui.py` 使用 PyQt6 而非 PyQt5
