- [x] `logging_config.py` 已创建，包含 `setup_logging()` 函数，配置统一格式
- [x] `gui_app.py` 的 `main()` 调用 `setup_logging()`
- [x] `robot_controller.py` 使用 logger 替代 print，方法内部无重复 `import re` / `import time`
- [x] `vision_system.py` 使用 logger 替代 print，检测中间数据用 DEBUG 级别
- [x] 所有其他模块（modbus_server/client, force_arc_controller, force_feedback_monitor, config_manager, workers, battery_monitor, gripper_controller, realtime_feedback_dialog, visual_servo_controller, depth_processor）使用 logger 替代 print
- [x] `dobot_api.py` 的 `wait_reply` 循环接收完整响应，不再截断
- [x] `robot_controller.py` 的 socket 超时使用上下文管理器，异常时自动恢复
- [x] `gui_mixins/` 目录已创建，包含 `__init__.py` 和各 Mixin 文件
- [x] `DobotMainWindow` 通过多继承组合所有 Mixin，功能与拆分前一致
- [x] 全项目无 `print()` 调试语句残留（除 gui_app.py 中用户可见的状态输出外）
